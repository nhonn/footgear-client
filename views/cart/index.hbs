<div class="container">
  <br>
  <h5><span>Giỏ hàng của bạn:</span></h5>
  {{#if cart.items}}
  <table class="table">
    <thead class="thead-dark">
      <tr class="d-flex text-right">
        <th class="col-3 text-left">Sản phẩm</th>
        <th class="col-3"></th>
        <th class="col-2">Size</th>
        <th class="col-2">Số tiền</th>
        <th class="col-2"></th>
      </tr>
    </thead>
    <tbody>
      {{#each cart.items}}
      <tr class="d-flex align-items-center border-top">
        <td class="col-3 border-top-0 "><img src="{{this.img}}" style="max-width: 100%" /></td>
        <td class="col-3 border-top-0 ">{{this.name}}</td>
        <td class="col-2 border-top-0 text-right">{{this.size}}</td>
        <td class="col-2 border-top-0 text-right">{{this.price}} ₫</td>
        <td class="col-2 border-top-0 text-center">
          <button class="btn btn-primary" onclick="removeFromCart({{this.productID}}, {{this.size}})"><i
              class="far fa-trash-alt"></i>
          </button>
        </td>
      </tr>
      {{/each}}
    </tbody>
  </table>
  <div class="paper d-flex">
    <div class="form-inline">
      <label>Tổng hóa đơn:</label>
      <div class="col"></div>
      <h4 class="price">{{cart.total}} ₫</h4>
    </div>
    <div class="col"></div>
    <a class="btn btn-primary text-light" href="/gio-hang/thanh-toan">
      Thanh toán
    </a>
  </div>
  {{else}}
  Hiện bạn chưa chọn sản phẩm nào vào giỏ hàng.
  {{/if}}
</div>